from setuptools import setup, find_packages

setup(
    name='dataadderDB',
    version='0.1.0',
    packages=['dataadderDB'],
    install_requires=[
        # List dependencies here
        'sqlite3',
        'csv'
        'json'
        'sqlite3'
        'datetime'  
    ],
)
