from setuptools import setup, find_packages

setup(
    name='ashishIs',
    version='0.2',  # Increment the version number
    packages=find_packages(),
    install_requires=[
        # Specify any dependencies your library might have
    ],
)