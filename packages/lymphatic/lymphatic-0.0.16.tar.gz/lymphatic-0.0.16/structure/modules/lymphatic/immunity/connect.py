
'''
	plan:
		import lymphatic.immunity.connect as connect
		connection = connect ()
		if (connection):
			r = connection.r;
			c = connection.c;
'''

import lymphatic.immunity.variables as variables

def now ():
	if (variables.utilize):
		ports = variables.find ("ports")

	return False