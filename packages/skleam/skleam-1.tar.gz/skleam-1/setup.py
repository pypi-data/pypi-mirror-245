from setuptools import setup, find_packages

setup(name='skleam',
      version='1',
      description='none',
      packages=['skleam'],
      author_email='woreped685@hupoi.com',
      zip_safe=False)
