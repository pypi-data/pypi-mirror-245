[project]
name = "simuldb"
readme = "PYREADME.md"
classifiers = [
    "Programming Language :: Rust",
    "Topic :: Database"
]

[project.urls]
Documentation = "https://solidtux-rust.gitlab.io/simuldb"

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool]

[tool.maturin]
manifest-path = "simuldb/Cargo.toml"
